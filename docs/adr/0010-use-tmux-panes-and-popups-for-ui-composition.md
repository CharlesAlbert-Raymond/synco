# Use tmux panes and popups for UI composition

The default experience launches a persistent left sidebar pane, while create/delete/edit flows can run as tmux popups over the main work pane. This keeps Syncopate embedded in the user's terminal workspace, preserves focus on the working pane after navigation, and avoids forcing every interaction through the original full-screen TUI.
